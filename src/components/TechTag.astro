---
interface Props {
  text: string;
  class?: string;
}

const { text, class: className = '' } = Astro.props;
---

<span class:list={["inline-flex items-center px-3 py-1 rounded-full text-xs font-label bg-tertiary-container text-tertiary", className]}>
  {text}
</span>
